# Keep skill notes

Keep skills live in `.cursor/skills`. They are overlays, not physics. The person in chat outranks them. When product changes, update the skill in the same slice.

Do not keep a second copy of these rules in home skill folders that contradicts the repo. The repo copy is the source.

## Load

`keep-process`, `keep-skill-selection`, `keep-product`, `production-app`, `prose-copy`, `apple-macos`, plus the slice file (`keep-testing`, `keep-observability`, `keep-accessibility`, `keep-release`).

Public Axiom, EventKit, menubar, SwiftUI, Hallmark, Firecrawl, Hyperframes, Screenpipe, Vercel: consult only. Do not import their default architecture.

## Repo facts that used to be stale in this file

* CI uses `derivedDataPath`. Debug `test` and Release `build` on `macos-15`.
* `KeepLog` and `OSSignposter` exist.
* Settings have `keep.schemaVersion`.
* Signing is still ad hoc. Notary is not claimed.

## Hazard already hit

A class swap crash was rewritten as “extra fields cannot type.” Energy folklore became fullscreen pause, Low Power Mode pause, and a 24 fps cap. Those are gone from the product. Do not put them back unless the user asks.
